local M = {
  executor = require("nb-nvim.client.executor"),
  note = require("nb-nvim.client.note"),
  notebook = require("nb-nvim.client.notebook"),
  config = require("nb-nvim.config"),
}

return M
